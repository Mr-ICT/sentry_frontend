# SENTRY Dashboard — environment variables
# Copy this file to .env and fill in the values.

# Production API base URL (e.g. https://api.sentry.example.com/api/v1).
# Only consulted when NODE_ENV !== 'development'; dev hardcodes http://127.0.0.1:8000/api/v1.
NEXT_PUBLIC_API_BASE_URL=
